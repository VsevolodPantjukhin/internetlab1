import { MainPage } from '@/pages/MainPage';
import { useRef } from 'react';

const App = () => {
  return (
    <div style={{ width: '100' }} className="App">
      <MainPage />
    </div>
  );
};

export default App;
